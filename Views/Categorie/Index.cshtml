@model IEnumerable<AEDFirst.Models.CATEGORIES>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomCat)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomCat)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCat }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCat }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCat })
        </td>
    </tr>
}

</table>
